import styled from 'styled-components'

export const StyledChangeTheme = styled.div`
    position: absolute;
    margin: 20px;
    top: 0;
    right: 0;

    h5 {
        display: inline-block;
        vertical-align: middle;
        margin: 0;
    }

    button {
        background-color: transparent;
        color: black;
        border: none;
        margin-left: 10px;
        vertical-align: middle;
        cursor: pointer;
        transition: 0.2s;

        &:hover {
        }
    }
`
